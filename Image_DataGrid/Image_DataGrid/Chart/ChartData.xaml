<UserControl x:Class="Image_DataGrid.Chart.ChartData"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Image_DataGrid.Chart"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="250">
    <Grid>
        <ListView>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"></StackPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListViewItem Width="105" Height="100">
                <StackPanel Width="90" Height="90" x:Name="ImageItem" MouseLeftButtonUp="ImageItem_MouseLeftButtonUp"/>
            </ListViewItem>
            <ListViewItem Width="145" Height="100">
                <TextBox Foreground="Gray" Margin="10" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="PriceItem"/>
            </ListViewItem>
        </ListView>
    </Grid>
</UserControl>
